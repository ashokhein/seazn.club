# ai-schedule-gap close-out — live execution state

**Read this first after any compaction.** It is the authority on what is done,
what is in flight, and every decision already made. Do not re-derive, re-triage,
or re-ask.

- Triage + decisions: `docs/superpowers/specs/2026-08-05-ai-schedule-gap-triage-design.md` (`1dcb61b0`)
- Task-by-task plan: `docs/superpowers/plans/2026-08-05-ai-schedule-gap-closeout.md` (`9a667e82`)
- Mode: subagent-driven. Implementer (Opus, high) → Reviewer (Sonnet) → gap list → repeat until clean and green.

## Standing rules for this programme

- **Do NOT file new issues.** If something is wrong, fix it inline. If it is
  unclear, ask the owner. Only stop and escalate when a fix would widen the
  blast radius beyond the task's stated files.
- Every subagent brief must be **self-contained** — verbatim code, exact paths,
  acceptance criteria, what not to touch, the verify command, an output cap.
  Subagents must not have to re-read the repo to understand the task.
- Reviewer output is a gap list, not prose. Implementer fixes, reviewer re-runs.
- Never accept "done, tests pass" without the raw JSON counts pasted back.
- **Subagents WRITE e2e cases but never RUN them.** Playwright outlives the
  600s subagent watchdog; two agents died to it on the #404 programme. The main
  thread runs e2e in a background bash at the group boundary.
- **Owner asked for FULL edge coverage (2026-08-05).** Every task ships tenancy/cross-org negatives, input boundaries (empty array, over-max, explicit-false), period/state boundaries, and double-submit. Assert the *effect* (board unchanged, balance unchanged), not only the status code — a 200-no-op and a 404 look identical from the response alone. Edge lists sent to all three in-flight agents; see the Log.
- Every brief carries a **15-line output cap**: counts, paths, deviations,
  blockers. No file contents, no diffs.

## Status

| Issue | Group | Task | State |
|---|---|---|---|
| #388 | — | — | **CLOSED** 2026-08-05, evidence comment. Shipped in `94513cd5` (W5). Secondary "N warnings" ask also settled: `reviewRowCount` is `rows.length` of the same array (`ai-review.ts:87`). |
| #389 | — | — | **CLOSED** 2026-08-05, mitigated. `walletIds` scoping already in `credits.ts:415`; #390 removes the rest. Deliberately did NOT truncate — the shared schema holds `sync:sports` reference data. |
| #394 | C | 1 | **PHANTOM — no production bug.** `single ? divBoardFixtures : actions.board` takes the FALSE branch when `single` is null, so the competition board already receives the whole board. Code and comment agree and always have (both written in `0f374d7a7`). Proven by mutation: at HEAD the new test is green; mutated to `consoleFixtures(divBoardFixtures, …)` it fails with the exact reported symptom. Regression test committed as `37ed6f45` (`test:`, not `fix:`). Owner call 2026-08-05: **close as invalid with the proof; do NOT chase the original symptom** (no repro, and `ai-competition-console.tsx:697` falls back to `c.fixtureId.slice(0,8)`, so an unlabelled row is unreachable via this path). **CLOSED** as not-planned 2026-08-05 with the full proof. Group C is done — no production change shipped. |
| #386 | B | 2, 3 | **Task 3 DONE — `393e8738`.** `undoJointApply` now makes ONE competition-scoped call; console passes `competitionId`. Gate (mine): **575/575, 0 failed, 0 foreign**; joint-apply suite 18 tests; all 4 locales at 3189 keys, 9 undo keys present in every locale and none identical to English. **Two agent-found deviations, both real:** (1) the console's partial-undo RETRY sent only the failed divisions — a subset the new endpoint 422s by design; would have broken on the SECOND press after a partial failure. Now always sends the full anchor set (re-restoring an already-reverted division rewinds zero steps). (2) `SCHEDULE_APPLY_RESTORE_IN_PROGRESS` is raised by the APPLY route; restore's `withSessionLocks` blocks with no `lock_timeout`, so a second restore WAITS — being fixed now. Copy keyed on **status, not code** (the usecase throws bare `HttpError(404\|422)` with no code, so a code match would never fire): 409 → retry, keeps the button; 422 → "no longer undoable in one go", button withdrawn; 404 → "nothing left to undo", button withdrawn. **Tasks 2 + 2b DONE** — `3b7eee20`, `554523e3`, `26ff23d9`. Lock shipped as ruled: `withSessionLocks` in `lib/db.ts` (out-of-pool, `max:1`, client created INSIDE the try, `idle_timeout:10`, `:6543` refused), restore holds `joint:<competitionId>` session-level, apply takes it under `set local lock_timeout='5s'` → 55P03 mapped to 409 `SCHEDULE_APPLY_RESTORE_IN_PROGRESS`. **FIX 5 mutation confirmed RED** (deleting the lock line fails all 3 lock tests). Restore suite now **20 tests**. Main-thread gate: **3628/3677, 0 failed, 0 failedSuites, 0 foreign**; `DB_POOL_MAX` default still 5. **i18n: none owed** — the 409 is a server-side API error with no UI; **Task 3 owes a dictionary key if it surfaces `SCHEDULE_APPLY_RESTORE_IN_PROGRESS`.** Reviewer dispatched. Superseded detail below. | **TASK 2c DONE — `363ba0d5`. A THIRD defect found, and it was the worst of the three.** FIX 1 (TOCTOU): the event read + set validation moved inside `withSessionLocks`; only `confirm` and the duplicate-id check stay outside. Test drives it from `pg_locks` (`granted = false` waiter probe), not a sleep. Mutation RED (read hoisted back out → `expected undefined to match { status: 422 }`). FIX 2: `{ lockTimeoutMs }` → `set_config('lock_timeout')` + `SessionLockTimeoutError`, **5s** matching the apply side; mutation RED (timeout removed → hung the full 180s test timeout). Client needed NO change — `refusalOf` at `ai-joint-apply.ts:252` already returns `"retry"` for any 409, so **no dictionary key was owed after all**. **FIX 3, the deviation, verified by me:** the lock connection runs no statements during a rewind, so postgres.js closed it at `idle_timeout: 10` and the backend took the advisory lock with it — **probed directly, key gone at 10.4s**. Every joint restore lasting >10s ran UNLOCKED while the code claimed exclusion, and FIX 2's 409 would have evaporated with it. Fixed with a 4s unref'd keepalive cleared in `finally` (`LOCK_IDLE_TIMEOUT_S = 10`, `LOCK_KEEPALIVE_MS = 4_000`, both read by me at `db.ts:113-116`); mutation RED (13.05s, lock false). `openapi.ts` restore entry `errors: [404, 422]` → `[404, 409, 422]`. **Main-thread full gate: 5482 passed, 0 failed, 0 failedSuites, 618 suites, 0 foreign, no MUTANT markers left.** Reviewer dispatched on the commit.
| ~~#386 old~~ | B | 2 | **Task 2 DONE** — `3b7eee20` in `wt-ai-gap-b`. Both premises re-verified true (no restore route existed; `undoJointApply` still loops). New: `competition-schedule-restore.ts` + route + `RestoreCompetitionScheduleRequest` + 5 tests. Main-thread gate: 272/272, failedSuites 0, foreign 0, engine resolves inside worktree. Reviewer dispatched. **Task 3 (client switch) pending.** |
| #391 | B | 4 | **DONE — `71c91226`.** 97/97, 0 failedSuites; mutation (3→5, per-competition→per-division) reds `states the joint run's own hourly limit, and that it is competition-keyed`. **Limit read from CODE: `rateLimit(`ai-plan-competition:${competitionId}`, { max: 3, windowSeconds: 3600 })` — 3/hour per competition, SKIPPED when `confirmed !== null` so a confirmed preview does not respend. The plan's prose was right; its LINE NUMBER was wrong — `competition-schedule-ai.ts:2484`, not 1660** (my brief repeated the wrong number; the agent checked the code, which is the rule working). Deviation: the plan's `toMatch(/competition/i)` is vacuous (the article says "competition" a dozen times) — replaced with a sentence-scoped helper plus an anti-vacuity twin proving the pre-#391 block yields no claim. |
| #392 | B | 5 | **DONE — `0f4072d3`.** 178/178 help+dictionary, 0 failedSuites. **Found and fixed a genuinely stale help sentence:** *"undo restores them one at a time rather than in a single step"* — true before #386, false after it. So the guard now covers BOTH falsehoods: `FALSE_JOINT_UNDO_PATTERNS` (all-or-nothing) plus `RETIRED_JOINT_UNDO_LOOP_PATTERNS`, both gated on an exported `JOINT_UNDO_SUBJECT`. **The plan's patterns were UNSAFE as written** — plurality alone fired on two TRUE sentences in the same article (the batch discount's "scheduling them one at a time", and "Applying writes every division together"); the corpus test caught it, hence the subject gate. Unplanned second edit: `dictionary-copy-truth.test.ts` walks every exported RegExp in `copy-truth.ts` and demands a `KNOWN_POSITIVES` line, so 10 fixtures were owed or that suite reds. Mutations: revert article → RED `the real article is clean`; drop pattern[0] → RED `fires on the synonyms an editor would reach for`; disable the subject gate → RED two titles. No i18n owed (help tree is English-only, no UI string changed). |
| #385 | A | 6 | **BUILT** — `9aed92f7`. `resolveRungConfig()` + `RungConfigProvider` (no default, throws). Both board RSCs wrap. Agent also fixed **2 unlisted call sites** with the same silent fallback (`ai-console` CTA, `ai-competition-console` quote). RED 2 → 9 green. |
| #387 | A | 7 | **BUILT** — `8066c900`. `quoted_credits` in / `quote_mismatch` out, one helper, all 3 run paths, receipt line in 3 surfaces. RED 4 (one per path) → 12 server + 7 component. |
| #383 | A | 8 | **BUILT** — `e835d7a6`. Auto-run effect + `officialsAutoStarted` removed; pre-run card + button. Price untouched. RED 5 → 9. |
| #384 | A | 9 | **BUILT** — `84d386ab`. RED 5 → 9. **Edge 13/14 found a real defect IN THE PLAN**: the plan's literal `draftAsPlan` (`base = pack.prior.assignments`) blanks every slot a partial or empty prior omits, and contradicts the plan's own summary string. Shipped as a MERGE of prior over the solver draft, filtered to fixtures/roles the pack has. |
| — | A | review | **REVIEWED — one MAJOR, verified real by the main thread and FIXED as `15090b5f`.** `recordQuoteMismatch` opened its own `withTenant` AFTER the run's ledger row and was awaited bare at all three call sites (`schedule-ai.ts:2900`, `competition-schedule-ai.ts:2767`, `officials-ai.ts:1349`), inside functions whose outer wrapper re-throws once `claim.creditConsumed` is true — so a constraint or serialization error on the TELEMETRY insert turned a paid, successful run into a 500, contradicting the helper's own docstring. Caught **inside the helper**, not per call site, so a fourth run path cannot forget it; the catch does NOT suppress the return value (the comparison is pure and already true — a lost row is no reason to tell the caller their quote matched). New suite `ai-quote-mismatch-resilience.test.ts` mocks `@/lib/db`: **7/7 green**, mutation `throw err` reds exactly the two failure-path tests. Existing DB suite still 12/12; tsc 0, eslint 0. Everything else reviewed CLEAN (provider has no default and no `??` fallback, `quoted_credits` is `.positive().optional()` so 0-as-absent cannot bite, 4 locales genuinely translated, T9's merge correct against all four scenarios). **Group A is DONE — 5 commits, no PR yet.** |
| — | A | gate | **Gate (mine, full apps/web): 5487 passed, 2 failed, 0 foreign — both failures in `credits-*` which A never touched. PROVEN ENVIRONMENTAL: 14/14 green on a fresh DB (54351) at the same commit.** Accumulated wallet volume on the reused 54345. Reviewer dispatched. `tsc` 0; `eslint` 0 with 78 warnings vs 79 baseline. **T9 has no Playwright e2e by design** — the adopt chip needs a `lazy_unfilled` slot that `ai-fixture-server.ts` never produces; covered in `scripts/smoke.ts` with a control run + deliberately-opposite adoption instead. |
| #390 | D | 10, 11 | **BUILT** — `3d107d77` (anti-join) + `d6aebe1d` (N+1). Premises verified real. Agent counts: RED 15/16 → GREEN 16/16, RED 17/18 → GREEN 18/18; `src/lib/__tests__` 1623/1648 0 failed 0 failedSuites. Reviewer dispatched; main-thread gate pending on PG 54345. **Task 11 deviated deliberately:** `orgPlanKey` is a 7-arm read-time resolver (lapsed comp, past_due grace, trial backstop, incomplete, canceled, org suspension) whose docstring records 3 divergent copies already, so it was LEFT as one query per row; the batched N+1 is the `plan_entitlements` read, moved out of `grantMonthly` into `monthlyPerSeatByPlan()`, with a shared `grantMonthlyDelta()` holding the lock + key check once. `1+2N` → `N+2`. **No e2e, no smoke extension** — cron internal, no UI; `scripts/smoke.ts` never calls the sweep. **Reviewed CLEAN** bar one stale comment citation, fixed inline as `c341d80e`. Main-thread gate on my own PG 54345: **1623/1648, 0 failed, 0 failedSuites, 0 foreign**; eslint 0, tsc 0, drift gates clean. **Group D is DONE — 4 commits (`3d107d77`, `d6aebe1d`, `c341d80e`, `c63bd598`), no PR yet.** Edge pass added 6 of 7 (period boundary, mixed sweep, unscoped anti-join, `walletIds: []`, soft-deleted org, `quantity_paid` 0); edge 2 skipped as already covered. **No defect found — `credits.ts` unchanged, confirmed by diff.** Each new test was **mutation-proved individually**: a period-blind anti-join (`like 'monthly:%'`) reds ONLY the period-boundary test, so that gap was real and nothing else caught it. `walletIds: []` was the suspect and is already correct — postgres.js renders `sql([])` as `(null)`, so `s.id in (null)` matches zero rows rather than raising `42601`. Final gate (mine, PG 54349): **1629/1654, 0 failed, 0 failedSuites, 0 foreign**; cron suite 18 → 24 tests. |
| #382 | E | 12, 13, 14, 15 | **BUILT — 5 commits** (`aac50f31` T12, `e0657848` T13, `5f90df38` T14, `35ca6cb3` T15, `86ca903f` follow-through). Agent gate: `vitest run src` success true, 5512/5562, 0 failedSuites. tsc 0, `typecheck:scripts` 0 (it caught a `cp3` redeclare in smoke.ts), eslint clean, both drift gates clean. Every task mutation-proved: T12 revert-rows-in-DB reds all 5; T13 `n>=limit`→false reds 7 and `drop=n-limit+1`→`1` reds exactly "post-insert count equals the limit exactly"; T14 `offset 3`→`30` reds 5 and dropping the `division_id` predicate reds "pruning is per division"; T15 restoring "1 free" reds 2. **Five plan corrections, all verified by the agent against the code:** (a) **V303 cannot be edited** — Flyway validates checksums here (no `validateOnMigrate=false`), PROVEN by a comment-only change failing `Migration checksum mismatch for migration version 303`; the correction went into V353's header per the plan's own fallback. (b) `listCheckpoints` is **newest-first**, so every ordering assertion the plan wrote was reversed. (c) **`RETURNING` does not preserve a subquery's `ORDER BY`**, so the plan's `gone.at(-1)` picks an arbitrary row — now returns `created_at, seq` and re-sorts in JS. (d) **The biggest one:** granting `scheduling.multi_division` to Event Pass makes it a PASS-LIFTED key, which the repo's own `pass-scoping-guard` immediately red-flagged at **4 org-wide gate sites** — an Event Pass holder would have paid and still been denied the competition-wide board. All four now resolve the competition in scope. It also forced the key into `PASS_FEATURES`, without which the paywall showed a Pro-only card to someone a $29 pass would clear. (e) Three suites + one smoke check encoded the REMOVED paywall (`schedule.test.ts`, `schedule-plus.test.ts`, `upgrade-gate.test.tsx`'s `NOT_LIFTABLE` fixture, `scripts/smoke.ts`) — inverted, not deleted, each inverted assertion reading the stored value back. Help copy was independently stale: `undo.md` said "Community keeps 1 save point". **Main-thread gate: 5512/5562, 0 failed, 0 failedSuites, 624 suites, 0 foreign — matching the agent exactly.** First attempt was 1724 failures across 579 suites: the agent had STOPPED its own PG 54353 on exit, so my re-run had no database. Fresh PG on 54355 (`show data_directory` verified mine), `db:apply` → 192 migrations at v353, `sync:sports` → 11 sports / 31 variants. Reviewer dispatched. **OWED: e2e not run** (`e2e/open-scheduling.spec.ts` new + `pro-plus-tier.spec.ts` edited, both written and unexecuted) **and no screenshots** — the eviction notice is unverified at desktop and 375px (a plain wrapping `<p>`, no fixed width or `whitespace-nowrap`, so scroll is unlikely, but that is reasoning, not evidence). |

## Execution order

```
C (task 1)  →  B (tasks 2-5)  →  A (tasks 6-9)  ∥  D (tasks 10-11)  →  E (tasks 12-15)
```

- **A is ONE implementer pass** (batching rule): tasks 6-9 all edit `ai-console.tsx` and the quote surfaces.
- **A ∥ D** is safe: D touches only `lib/credits.ts` + its tests. No UI, no locale strings, no OpenAPI.
- **E must follow A.** They collide on `ai-console-state.ts` and on `lib/i18n-keys.ts` (both add locale strings; `i18n:gen-keys` regenerates it).

## Worktrees

| Group | Path | Branch | npm ci | engine resolves inside |
|---|---|---|---|---|
| C | `/Users/ashokhein/github/wt-ai-gap-c` | `ai-gap-c-board-wiring` | ✅ | ✅ verified |
| B | `/Users/ashokhein/github/wt-ai-gap-b` | `ai-gap-b-joint-undo` | ✅ | ✅ verified |
| D | `/Users/ashokhein/github/wt-ai-gap-d` | `ai-gap-d-cron-antijoin` | ✅ | ✅ verified |
| A | `/Users/ashokhein/github/wt-ai-gap-a` | `ai-gap-a-quote-integrity` | ✅ | ✅ verified |
| E | `/Users/ashokhein/github/wt-ai-gap-e` | `ai-gap-e-open-scheduling` | ✅ | ✅ verified |

Worktree E is branched off **A's tip (`84d386ab`), not `main`** — deliberately.
Both regenerate `i18n-keys.ts`, and branching from `main` guarantees a conflict
there at merge. E is therefore one commit behind A's `15090b5f`; rebase before PR.

PG ports in use by this programme: 54341 (B), 54345 (A), 54349 (D), 54351 (D
proof), 54353 (E). Do not reuse — a `createdb` against a foreign server succeeds.

All have `.env.local`, `apps/web/.env.local`, `.claude/agent-memory` symlinked to the main checkout.
Creation script for A and E is in the plan's Appendix.

## Owner decisions — settled, do not re-ask

| # | Decision |
|---|---|
| 388 | Close with evidence comment. Done. |
| 389 | Close as mitigated. No truncate. Done. |
| 382 | ONE PR: entitlement matrix + rolling checkpoint eviction + AI-anchor pruning. |
| 383 | Do NOT auto-run. Show the card at its flat 1 credit; organiser presses the button. Price stays 1 — do not change `freeDraftQuote`. |
| 384 | Seed the solver with `prior.assignments`. `prior.instruction` is IGNORED on the empty path — nothing is re-executed. Do NOT also change the priced adopt path. |
| 385 | Resolve weights + budgets server-side, provide via context. Provider throws with no default. |
| 386 | Server-side, locked, **best-effort** — NOT one transaction. Client sends the checkpoint pairs; server validates the division set equals the apply event's `division_ids` exactly, rejecting a subset with 422. |
| 386-lock | **2026-08-05, after review.** Task 2 shipped a NO-OP lock: `withTenant(… lockDivisions)` commits immediately and `pg_advisory_xact_lock` releases at transaction end, so nothing was held across the restore loop. Owner chose **session-level `pg_advisory_lock` on a pinned connection, released in a `finally`** — a real lock that blocks a concurrent joint apply for the whole restore. Rejected: dropping the lock and documenting honestly; and reversing to one transaction. Key derivation MUST collide with `lockDivisions`' or the exclusion is fake. |
| 386-deadlock | **2026-08-05, PROVEN.** The session lock on `division:X` **cannot** work: `restoreCheckpoint` → `undoDivision` → `step()` (`history.ts:175`, also `:446`/`:475`) takes `pg_advisory_xact_lock(hashtext('division:'+id))` on a POOL connection once per undo step. Session and xact advisory locks share one lock space, so an out-of-pool session lock on that key blocks the restore's OWN rewinds forever (nothing sets `lock_timeout`). Demonstrated with two clients: `55P03` under a 3s timeout. Owner ruled: **a second, competition-scoped key namespace** — `applyCompetitionSchedule` takes `pg_advisory_xact_lock(hashtext('joint:'+competitionId))` at the start of its transaction (in addition to `lockDivisions`, unchanged); the restore holds `pg_advisory_lock` on the same key session-level, out of pool, for its whole run. Different namespace, so rewinds stay free. **Accepted limit: this excludes a concurrent JOINT apply, not a single-division edit** — those still interleave per step, as today. Rejected: a per-division `joint-div:` key covering the single-division apply paths too (widest blast); and dropping the lock. |
| 386-toctou | **IN FLIGHT** (Task 3 landed, worktree free). Review of `26ff23d9` found the "latest joint apply" read and the derived 422 division-set check (`competition-schedule-restore.ts:42-66`) run **before** `withSessionLocks` takes the `joint:` lock. A concurrent apply completing in that window is invisible to the validation; it only self-heals when the new apply touched the same divisions, via `restoreCheckpoint`'s watermark check (`history.ts:399-403`) — which the comments at `:68-91` do not claim. Not a regression (the ordering predates the lock), but it IS a hole in the guarantee the lock now advertises. **Fix: move the event read + set validation INSIDE `withSessionLocks`.** Needs a test that a joint apply landing between read and lock is caught. |
| 386-why | **2026-08-05.** Owner confirmed KEEP the lock (rejected "detect instead of prevent" and "drop it"), and clarified the operating model: **a single org owner** handles scheduling. So the lock's justification is NOT "two admins racing" — it is **double-submit / two tabs / retry-after-slow-response by one organiser**, which is the more likely race. Comments must say so, or a later reader concludes it is dead code under this product model and deletes it. Raises the value of the `lock_timeout` → 409 path (a double-submit gets "a restore is already in progress", not a stall). |
| 386-leak | **2026-08-05.** Owner asked what happens if the session lock is never released. Session advisory locks die with the backend connection, so a crash is self-healing; the gap is a live-but-abandoned connection. Two guards ordered: (a) short `idle_timeout` (~10s) on the dedicated lock client so an orphan self-closes — worst case becomes seconds, not forever; (b) `set local lock_timeout = '5s'` on the **apply** side before taking the `joint:` key, mapping `55P03` → a retryable **409** so a stuck lock is a visible error rather than a hung request. NOTE: the FIX 5 lock test stays timeout-free — a timeout there would mask the deadlock it exists to catch. |
| 386-pool | **2026-08-05.** The lock connection is **OUT OF POOL** — a dedicated short-lived client, not `sql.reserve()`. Reason: `withTenant` uses `.begin()`, so the restore needs a 2nd connection for its rewinds while pinned; at `DB_POOL_MAX` 5 that starves the pool, and this repo has already had "a transaction deadlocked all DB access at DB_POOL_MAX=5" (`docs/superpowers/plans/2026-07-21-billing-groups-remaining.md:64`). Owner **rejected raising the default to 25**: `fly.toml:43` + `design/v8/prompts/PROMPT-54-db-connection-budget.md:33` define the budget `machines × DB_POOL_MAX + Flyway(1) + ops(3) + Supabase(~12) ≤ limit` (stg 5 / prod 8, stg has 60 slots), and `db.ts:82` records the 2026-07-13 FATAL 53300 outage from holding 25+. |
| 387 | Inline receipt line (both directions) **plus** a server-side `schedule.ai_quote_mismatch` competition_event. Detection is server-side: the client sends `quoted_credits`, the server compares. |
| Order | C → B → (A ∥ D) → E. |
| Test bar | unit + E2E + smoke + regression on all, EXCEPT tasks 4 and 5 (help article, copy-truth guard) = unit + regression only. |

## Facts established by scouting — do not re-scout

- **`ai-joint-run.ts` does NOT call the rung weight functions.** Only `isRung`/`Rung`. The issue text is wrong. #385's client surface is `ai-quote-card.tsx` (which owns `quoteFor`) and `ai-officials-review.tsx:214`.
- **`quoteFor` is NOT in `ai-rung.ts`.** It is `ai-quote-card.tsx:89-92`, and already takes an optional `weights` — the seam exists.
- **`schedule-board.tsx` is `"use client"` (line 1).** "Thread from the server" means a provider seeded by the RSC that renders the board.
- **`restoreCheckpoint` is NOT a transaction.** `history.ts:379` loops up to 500 `undoDivision` calls, each its own transaction, deliberately ("single-writer append, concurrency-safe").
- **`schedule.applied_multi` carries `division_ids` only.** No checkpoint ids. Anchors live client-side as `JointCheckpoint {divisionId, checkpointId}` (`ai-joint-apply.ts:42`).
- **`undoJointApply` returns `{ok, failed}`.** There is no `undonePartial` field in that file.
- **The wallet id IS the subscription id.** The sweep selects `s.id`; `grantMonthly` builds `monthly:${walletId}:${period}` from it. Anti-join needs no extra join.
- **`orgPlanKey` is imported from `@/lib/entitlements`**, not defined in `credits.ts`.
- **`monthlyPeriod()` is module-private** in `credits.ts:184`, `YYYY-MM` UTC. Already in scope for the sweep.
- **Community save-point cap is 2**, set by V319 (V290's 1 is superseded).
- **Next free migration is V353.** `V344` is taken by `V344__org_has_feature_pass_on_any_plan.sql`.
- **Help tree is `apps/web/content/help/`**, not `content/help/`. English-only, no i18n owed.
- **Dictionaries** are flat dotted-key JSON at `apps/web/src/dictionaries/{en,es,fr,nl}/ui.json` (8 files per locale; `ui.json` is the one these tasks touch).
- **Telemetry has no helper.** Events are raw `insert into competition_events` at each call site.
- **Any new `app/api/v1/**/route.ts` MUST get a `key-scopes.ts` entry.** `key-scopes.test.ts` enumerates routes off disk and fails any route that is neither allowlisted nor never-listed. The plan omitted this; Task 2 hit it. Mirror the sibling route's scope + pin.
- **`Uuid` is the house zod primitive** (`schemas.ts:16`), not `z.string().uuid()`.
- **The symlinked `.env.local` points tests at the owner's dev DB (`localhost:5432/seazn`), which is BEHIND on migrations.** A full `src/server/usecases/__tests__` run there gives ~464 failures, all `column/relation does not exist` (`merged_into`, `registrations.user_id`, `config_snapshot`, `persons.lane`, `ai_parse_previews`). NOT a regression. Do NOT migrate the owner's DB — stand up a scratch PG per the `seazn-local-env` skill. **A scratch server is live for this programme: `postgresql://postgres@127.0.0.1:54341/seazn_test`, `DATABASE_SSL=disable`, at V352 + `sync:sports` done. Reuse it.**
- **Prod runs the Supabase SESSION pooler on `:5432` (Supavisor)** (`fly.toml:39`) — a backend is pinned per client connection, so session-level `pg_advisory_lock` is SAFE there. The code also supports the **transaction** pooler on `:6543` (`db.ts:44` turns off prepared statements for it), where a session lock silently breaks: consecutive statements can land on different backends. Any session-level lock must refuse to run against `:6543`.
- **`lockDivisions` key derivation is single-arg:** `pg_advisory_xact_lock(hashtext('division:' || id))` (`competition-schedule-apply.ts:202-206`), sorted via `lockOrder` = `[...new Set(ids)].sort()`. A session lock must use `pg_advisory_lock(hashtext('division:' || id))` — same single-arg overload, same key string — or the mutual exclusion is fake.
- **`withTenant` sets `app.current_org` + `role app_user` with `set_config(…, true)` / `set local`, both TRANSACTION-scoped** (`db.ts:99-110`). They do not survive on a pinned connection between transactions.
- **No session-level advisory lock and no `sql.reserve()` exists anywhere in the repo** — there is no precedent to copy. Driver is postgres.js `^3.4.9` (`apps/web/package.json:42`).
- **E2E home is `apps/web/e2e/ai-architect.spec.ts`** for every AI-console case. Mobile is a Playwright project (`--project=mobile-se`), not a manual screenshot. Smoke is `npm run test:smoke` → `scripts/smoke.ts`.

## Existing tests these tasks will break — update deliberately

- `ai-architect.spec.ts:509` — "the officials step prices itself: free draft with no picker" → **Task 8** removes the auto-run.
- `ai-architect.spec.ts:197` — "pro: brief → run → CLEAN → officials → apply → undo" → **Task 8** (extra click) and **Task 3** (undo is one call).
- `history.test.ts:273-282` — asserts the checkpoint 402 → **Task 13** replaces the 402 with eviction. Change it, do not add a contradicting test.

## Verification traps that produce a false green here

- Judge vitest ONLY from `--reporter=json --outputFile`. A suite that fails to **collect** reports `numFailedTests: 0` — read `numFailedTestSuites`.
- **NEW 2026-08-05: `npx vitest run` with 12+ positional file filters matches ZERO files** and reports `0/0, files 0` with an empty log. Two positionals are fine. Use directory filters for anything wider. (Found by the Group D agent.)
- **`eslint`/`tsc` must be run from `apps/web`, not the repo root.** From the root, eslint exits **2** with "couldn't find an eslint.config file" and tsc prints its usage banner — both look exactly like a regression. Hit this myself on the Group D comment fix.
- `rtk` prints `PASS(0) FAIL(0)` for a collection failure and hides lint output.
- Shell cwd resets to the main checkout between calls. Prefix `cd <abs worktree> &&` in the SAME call as anything you judge.
- A worktree without `.env.local` skips ~1772 DB tests while `total` stays unchanged — only `pending` moves.
- A symlinked `node_modules` compiles MAIN's engine. `readlink -f node_modules/@seazn/engine` must resolve inside the worktree.
- Never `git stash` in a worktree — the stash stack is shared with the main checkout.
- Pre-commit, every commit: `npm run openapi:gen && npm run i18n:gen-keys`, then `git status --porcelain` empty. Both gates are CI-only.

## Log

- 2026-08-05 — triage complete, 12 issues verified against `1ee962f0`. #388 and #389 closed. Spec + plan committed. Worktrees C/B/D created; C has `npm ci`.
- 2026-08-05 — edge-coverage sweep ordered by owner. #386: 14 edges (cross-org checkpoint/competition, mismatched checkpoint↔division, empty + >20 arrays, explicit `confirm:false`, all-fail, missing `division_ids`, double restore, deadlock regression, lock released after a FAILED restore, `:6543` guard, blocked-apply 409). Group A: 16 edges (credits >6 fallback, all THREE mismatch call sites, `quoted_credits` 0/negative vs omitted, double-press → one POST, partial/empty/stale `prior.assignments`). Group D: 7 edges in a dedicated pass on PG 54347 — **period boundary is the highest-value one** (a bug there silently stops all monthly grants forever and no existing test would catch it).
- 2026-08-05 — #394 closed as invalid (group C done, no production change). Task 2 landed `3b7eee20`; main-thread gate 272/272. Worktree A created off main tip `6ef3989b` (main advanced by a docs commit from outside this session) — `npm ci` done, engine resolves inside. Group D dispatched. Gotcha: `.git/worktrees/<name>/info/` does not exist on a fresh worktree, so appending to its `exclude` needs `mkdir -p` first (the shared `.git/info/exclude` already carries `.claude/agent-memory`, so this is belt-and-braces).
- 2026-08-05 — Group A reviewed. The one MAJOR was real: telemetry that could 500 a paid run. Fixed inline as `15090b5f` (helper-level catch + a mocked-`withTenant` suite, mutation-proved). Group A closed at 5 commits. Worktree E created off A's tip and Group E (#382, tasks 12-15) dispatched on PG 54353 with the V344→V353 correction in the brief. Tasks 4 and 5 still queued behind Task 2c, which owns `wt-ai-gap-b`.
- 2026-08-05 — #386 took THREE rounds, each fix exposing the next layer: a lock that held nothing (Task 2) → a lock that deadlocked against its own rewinds (2b) → a lock that silently let go at 10s (2c). The pattern worth remembering: each round's fix was correct and each round's verification was honest; what kept failing was the assumption that a lock, once taken, stays taken. Only a direct `pg_locks` probe past the idle timeout found round three. Tasks 4+5 dispatched as one pass; reviewer dispatched on `363ba0d5`. Group E still in flight.
- 2026-08-05 — **#386 round FOUR.** The 2c reviewer found the keepalive is itself unsound, and I verified both MEDIUMs against the code. (1) postgres.js 3.4.9 reconnects transparently — `connection.js` `reconnect()` on close, `connect(query)` requeues — so the beat's `select 1` proves *a* connection is alive, NOT that *this backend* still holds the key. The lock connection is idle >99% of the time, so a drop lands between beats: the next ping opens a NEW backend, succeeds, and the rewind continues believing it is excluding an apply, with `pg_advisory_unlock_all()` then running on the wrong backend. **The keepalive made the round-three failure invisible rather than fixing it.** (2) The beat removes the bound that used to cover a wedged socket: `unlock_all` is awaited unbounded, the outstanding beat keeps `idle_timeout` from ever arming, so the request hangs until the gateway kills it. Plus 3 MINORs (stale route contract comment still says "NO 409 escapes this route"; a 4s `waitUntil` that can flake against the 5s `lock_timeout`; a 404-vs-422 precedence flip the commit message claimed was unchanged).
  **Ruling (mine, inline — the endpoint already has a partial-report contract, so this does not widen blast radius):** beat on `pg_backend_pid()` and compare against the pid captured at acquisition; on mismatch or ping rejection set a lost-lock flag. Do NOT reject after `fn` settles as the reviewer proposed — the rewinds are already written and the partial report is TRUTHFUL about them; discarding it leaves the client with no record. Instead surface the flag INTO the loop: check before each division, stop early, and report the untouched ones in `failed[]` so the client's existing retry path handles it. Bound the `finally` unlock too.
- 2026-08-05 — Tasks 4 + 5 DONE (`71c91226`, `0f4072d3`). Both agent deviations were the agent being right about a plan that was wrong — a vacuous `toMatch(/competition/i)`, and guard patterns that fired on two TRUE sentences. Round-four fix for #386 dispatched into the now-free worktree B with the lost-lock design ruled in the brief.
- 2026-08-05 — Group E built, 5 commits. The pass-scoping guard earned its keep: opening `scheduling.multi_division` to Event Pass would have left a paying pass holder denied at 4 org-wide gate sites, and the repo's own guard caught it before any human did. Reviewer dispatched. **Two debts now tracked for the group boundary: e2e (never run this programme) and 375px screenshots for T13's eviction notice.**

## Trap learned 2026-08-05 — an agent that tears down its scratch DB invalidates the main-thread re-run

Group E's agent stopped its PG 54353 when it finished. My independent gate then
reported **1724 failures across 579 of 624 suites, `pending` 50 → 220** — a
shape no five-commit diff can produce, and the tell that it is environmental
rather than a regression. The re-run exists precisely to be independent, so an
agent tearing down the environment destroys the thing it is meant to enable.
**Every future brief: leave your Postgres RUNNING and report its port.**
- 2026-08-05 — **Group E reviewed: one IMPORTANT + three MINORs, all verified by me against the code.**
  **IMPORTANT (`ui.json:885`, all 4 locales):** `upgrade.proCard.body` says Pro "adds the schedule board, player stats, officials and API access **the pass never covers**". It is rendered by `c/[compSlug]/upgrade/page.tsx:934` to a community org with no pass — i.e. EXACTLY the organiser who just hit the `scheduling.multi_division` gate and landed on `?feature=scheduling.multi_division`. Since V353 the $29 pass DOES cover the competition-wide board. The card argues against the purchase `PASS_FEATURES` was just extended to offer. Confirmed verbatim by me.
  **MINOR `history.ts:362`:** the `limit === 0` arm breaks the stated "post-insert count is exactly the limit" invariant — `n=0, limit=0` gives `drop=1`, deletes nothing, insert still lands, so an org whose `schedule.checkpoints.max` resolves to 0 (staff override, or a MISSING row — `getLimit` returns 0 for both) permanently holds one save point instead of none.
  **MINOR `history.ts:355-390`:** count-then-delete is not serialised, unlike `applySchedule`. Two concurrent POSTs at the cap both target the same oldest row; the loser deletes 0, both inserts land, the division sits at `limit + 1` and one organiser is never told a bookmark went. Self-correcting next save. **Given the single-org-owner assumption this is the double-click case, so it is worth the advisory lock.**
  **MINOR `ai-scheduling.md:177`:** "The multi-division board is a **Pro** feature" — now incomplete, an Event Pass lifts it for one competition. Same lost-sale shape as the IMPORTANT. English tree, no i18n owed.
  Reviewer confirmed CLEAN and load-bearing: `pass-scoping-guard.test.ts` AST-scans every non-test source file, so the four fixed sites ARE the complete literal set; V353 is idempotent and all six plan keys resolve true; the prune is genuinely per-division; **every inverted test reads state back rather than asserting a constant** (the thing I flagged as the risk of inverting rather than deleting).
- 2026-08-05 — **e2e RUN at last** (the debt outstanding all programme). Prod build of worktree E, standalone server on :3100, PG 54355. First run: **16 passed, 2 failed**; after fixes **`open-scheduling` 7/7 and `pro-plus-tier` 9/9**, `E2E_EXIT=0`. Spec fixes committed as `b3a15a23`. **Every failure was the SPEC, not the product** — the screenshots prove the feature: a Community org gets the full board with no paywall, and the eviction notice renders *"before rain …" was replaced — your plan keeps 2 save points. Undo still rewinds past it* with exactly the two survivors listed.
  **The class is predictable and WILL recur, because a subagent writes e2e it is forbidden to run.** Four defects, all mechanical: (1) waited on a `History` HEADING and the save-point input on the DEFAULT board tab — both live in the HistoryPanel, mounted only for `?tab=history`; (2) `getByRole` matches an accessible name by SUBSTRING, and two info buttons are labelled "About: Undo and save points" / "About: Save points", so `{ name: "Save point" }` resolved to THREE elements; (3) asserted the evicted label was gone from the PAGE three lines before asserting the notice NAMES it — self-contradictory, since the notice quoting it IS the feature; (4) my own first fix used heading "Constraints" when the real text is "Constraints & planning" — caught before running, not after.
  **Fold into every future e2e brief:** prefer `exact: true`; anchor readiness on an element you have READ in the markup, not one you expect; and when a feature's whole point is that some string appears in a notice, scope the absence assertion to the list.
  Also added, unasked but owed: the notice is now asserted at **375px** with a document-level horizontal-scroll check plus a screenshot artifact, rather than eyeballed once — a future copy edit that overflows fails in CI. Screenshot verified by me: wraps to two lines, cards stack, no page scroll.
- 2026-08-05 — **#386 round FIVE — `1effc7a1`.** The two older lock tests were flaking ~1 run in 3: the whole restore resolves in **31ms** while `waitUntil` polled every **25ms** — a pre-existing sampling race, not caused by round four. The agent stopped and ASKED rather than rewriting two passing tests unbidden, which was right. My ruling: a 31ms window sampled every 5ms is ~6 samples — a narrower race, not a fixed one, and it will fail on a loaded CI runner where it never fails here. **A test green two runs in three is worse than no test, because it teaches everyone to re-run until it passes.** Fixed deterministically via the `parkInsideLock()` interpose seam already built for the mid-rewind case. **Five consecutive full runs, 37/37, `success=true` every time.** Every existing assertion kept; the agent flagged the one nuance himself rather than burying it (test 12 now observes the division key while the joint key is held with the body parked, instead of while rewind steps run — still valid, because `withSessionLocks` acquires all keys before the body). Also confirmed AND asserted, not assumed: on a mid-loop loss the response keeps the rewound division in `restored[]` with real `steps`/`watermark`, only the untouched one in `failed[]`, `ok: false`, surviving the wire round-trip — the partial-truthfulness that was the whole reason I ruled against rejecting after the body settles. Fifth reviewer dispatched; four wrong rounds earns one more look. **Main-thread full gate on the CLEAN PG 54359: 5499 passed, 0 failed, 0 failedSuites, 618 suites, 0 foreign — no caveats.** Group B is 7 commits (`3b7eee20`, `554523e3`, `26ff23d9`, `393e8738`, `363ba0d5`, `e67eb0fc`, `1effc7a1`) plus `71c91226` and `0f4072d3` for #391/#392.
- 2026-08-05 — **The credits reds are environmental, and now PROVEN rather than waved off** (they recurred on worktree B's gate: 5497 passed, 2 failed, 4 failedSuites). Isolation run on the SAME reused DB 54341: 14/14 green — but at **29,638ms and 29,810ms against a 30s timeout**, i.e. passing by 0.4s, which is the documented "duration is a clock, not a bug" signature rather than a clean result. Fresh PG 54359 (`show data_directory` verified mine, `db:apply` + `sync:sports`), same commit, same suites: **14/14 in 91ms and 159ms**. A ~300x difference on identical code — accumulated wallet volume on a DB reused all programme, exactly as `reference_vitest_count_masking` and the local-env skill describe. Full gate re-running on 54359 so the number I record is honest rather than caveated. **Lesson worth keeping: an isolation run that goes green is NOT proof on its own — check the DURATION. Green at 29.6s of a 30s budget is the same defect as red at 30.1s, one lucky scheduling decision apart.**

## RULING 2026-08-05 — the joint-restore lock is being REMOVED, not fixed again

Round six found three more defects (pid captured a statement AFTER acquisition
so a drop in the gap records the WRONG backend; `end()` sends a graceful FIN a
black-holed socket never delivers, so the backend keeps the key for ~2h; and
`set_config('lock_timeout')` is not replayed on reconnect). All three verified
against the code and postgres.js source.

**The owner's question settled it: "are you suggesting to block double click?"**
I checked — and `ai-competition-console.tsx:1096` ALREADY guards it with an
`undoInFlight` ref plus `setUndoing(true)`. The lock's own comment justifies
itself with "the SAME organiser submitting twice. A double-clicked Undo…", and
that case was NEVER reachable. **I had repeated that justification several times
without verifying it.** What actually remained was a second tab or a
reload-retry — real, but rare.

Decision: replace the lock with an apply-event ANCHOR RECHECK before each
division. Rationale:
- The lock failed OPEN and INVISIBLY in four of six rounds, each time claiming
  exclusion it was not providing. An id comparison cannot fail that way.
- The reviewer established the lock's protection was ALREADY per-division (a
  loss inside one `restoreCheckpoint` is unseen until the next iteration), so
  the recheck has the SAME granularity. Same protection, no liveness assumption.
- What is lost is PREVENTION: a concurrent apply no longer gets a clean 409, and
  the restore aborts partway instead. Accepted — the partial report is the
  endpoint's existing contract, and the client's retry path already handles it.

Deleting: `withSessionLocks` + `SessionLockTimeoutError` + keepalive + pid
tracking + bounded unlock from `db.ts` (restore was the ONLY production
consumer), the apply's `joint:` xact lock and its 409, and both openapi
summaries' lock prose. **Generalise: a defensive mechanism whose failure mode is
silent and fail-open is worth less than its comments claim. Prefer a check that
cannot be quietly inert.**
