// Generic fallback module ≈ v1 behaviour (win_loss / score modes) — spec 03 §1,
// spec 04. Keeps every existing v1 competition foldable under v2. Implemented
// in PROMPT-03 (contract) / PROMPT-15 (cutover parity).

export {};
