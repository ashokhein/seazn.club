// Register/resolve SportModules by key + version — spec 03 §3 (registry &
// versioning). Divisions pin module_version at creation; old versions stay
// importable while live divisions reference them. Implemented in PROMPT-03.

export {};
